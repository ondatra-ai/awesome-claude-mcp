services:
  frontend:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./services/frontend:/app
    command: sh -c "npm install && npm run dev -- --hostname 0.0.0.0"
    ports:
      - "3000:3000"
